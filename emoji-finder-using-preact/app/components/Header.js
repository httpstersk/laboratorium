import { h } from 'preact';
import { Link } from 'preact-router';
import mdl from 'material-design-lite/material';
import { Layout, Navigation } from 'preact-mdl';

export default () => (
    <Layout.Header fixed-header fixed-drawer>
        <Layout.Title></Layout.Title>
    </Layout.Header>
);